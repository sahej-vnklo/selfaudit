export const METRIC_DEFINITIONS = {
  daily_revenue: {
    label: 'Net revenue',
    unit: 'currency',
    definition: 'Gross item revenue minus refunded item amounts over the trailing 30 days. Numerator is included order line item price × quantity less refund line item amounts; denominator is not applicable. Uses the store primary currency only; orders in other currencies are skipped with a console.warn. Exchanges represented as refund+reorder reduce the original order and count the reorder as new revenue. Excludes cancelled-before-fulfilment orders and test orders.',
    window_days: 30,
    sources: ['shopify'],
    exclusions: ['cancelled orders', 'test orders', 'non-primary currency orders'],
  },
  refund_rate: {
    label: 'Refund rate',
    unit: 'percent',
    definition: 'Refunded order count ÷ total order count over the trailing 30 days, ×100. Counts orders with any refund, full or partial. Exchanges represented as refund+reorder count as refunds on the original order. Uses the store primary currency only; orders in other currencies are skipped with a console.warn. Excludes cancelled-before-fulfilment orders and test orders.',
    window_days: 30,
    sources: ['shopify'],
    exclusions: ['cancelled orders', 'test orders', 'non-primary currency orders'],
  },
  refunded_amount_30d: {
    label: 'Refunded amount',
    unit: 'currency',
    definition: 'Sum of refund line item subtotals over the trailing 30 days. Only explicit refund amounts count (refund line subtotal/total/amount, or refund transaction amounts) — refund dollars are never reconstructed from list prices, because a partial refund estimated at full price would overstate measured loss. If any refund in the window lacks an explicit amount, this metric is not emitted and financial impact reports tier "none". Exchanges represented as refund+reorder count the refund amount on the original order. Uses the store primary currency only; orders in other currencies are skipped with a console.warn. Excludes cancelled-before-fulfilment orders and test orders.',
    window_days: 30,
    sources: ['shopify'],
    exclusions: ['cancelled orders', 'test orders', 'non-primary currency orders'],
  },
  aov: {
    label: 'Average order value',
    unit: 'currency',
    definition: 'Gross item revenue before refunds ÷ total order count over the trailing 30 days. Numerator is included order line item price × quantity before refund deductions; denominator is included orders. Uses the store primary currency only; orders in other currencies are skipped with a console.warn. Exchanges represented as refund+reorder leave the original gross order value intact and count the reorder as a separate order. Excludes cancelled-before-fulfilment orders and test orders.',
    window_days: 30,
    sources: ['shopify'],
    exclusions: ['cancelled orders', 'test orders', 'non-primary currency orders'],
  },
  orders_count: {
    label: 'Orders',
    unit: 'count',
    definition: 'Count of fulfilled or fulfilment-eligible orders over the trailing 30 days. Numerator is included orders; denominator is not applicable. Refunds and exchanges do not remove an order from the count unless the order was cancelled before fulfilment. Uses the store primary currency only; orders in other currencies are skipped with a console.warn. Excludes cancelled-before-fulfilment orders and test orders.',
    window_days: 30,
    sources: ['shopify'],
    exclusions: ['cancelled orders', 'test orders', 'non-primary currency orders'],
  },
  fulfilment_time_hrs: {
    label: 'Fulfilment time',
    unit: 'hours',
    definition: 'Median hours from order created_at to first fulfilment created_at for orders with fulfilment timestamps over the trailing 30 days. Numerator is the median elapsed hours; denominator is not applicable. Refunds and exchanges do not change the fulfilment timestamp calculation. Uses the store primary currency only; orders in other currencies are skipped with a console.warn. Excludes cancelled-before-fulfilment orders, test orders, and orders without fulfilment timestamps.',
    window_days: 30,
    sources: ['shopify'],
    exclusions: ['cancelled orders', 'test orders', 'non-primary currency orders', 'orders without fulfilment timestamps'],
  },
  repeat_rate: {
    label: 'Repeat purchase rate',
    unit: 'percent',
    definition: 'Orders from customer emails seen on an earlier included order ÷ total order count over the trailing 30 days, ×100. First order from an email is new; later orders from the same email are repeat. Refunds and exchanges do not change repeat status. Uses the store primary currency only; orders in other currencies are skipped with a console.warn. Excludes cancelled-before-fulfilment orders, test orders, non-primary currency orders, and orders without customer email from the numerator while keeping them in the denominator.',
    window_days: 30,
    sources: ['shopify'],
    exclusions: ['cancelled orders', 'test orders', 'non-primary currency orders'],
  },
  support_ticket_surge_ratio: {
    label: 'Support ticket surge ratio',
    unit: 'ratio',
    definition: 'Support tickets created in the last 9 days divided by support tickets created in the 9 days before that. The window anchors on the payload as_of timestamp when present, otherwise the newest ticket created_at. Emits only when the prior 9-day window has at least 3 tickets, so the ratio is not based on a near-zero denominator. Uses structured ticket created_at only; ticket text is not read.',
    window_days: 18,
    sources: ['zendesk', 'intercom', 'gorgias'],
    exclusions: ['tickets without created_at', 'prior windows with fewer than 3 tickets'],
  },
}
